//const fs = require('fs');
//const sharp = require('sharp');

//const inputDir = 'src/images/fullsize';
//const outputDir = 'public/images/resized';

//fs.readdirSync(inputDir).forEach(file => {
//  sharp(`${inputDir}/${file}`)
//    .resize(800) // Adjust dimensions as needed
//    .toFile(`${outputDir}/${file}`, (err, info) => {
//      if (err) throw err;
//      console.log(`${file} resized successfully`);
//    });
//});
